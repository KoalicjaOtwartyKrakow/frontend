import React from "react";

const HostListDescription = () => {
    // const { t } = useTranslation(["host"]);
    return (
        <article>
            <p>TODO</p>
        </article>
    );
};

export default HostListDescription;
